---
title: LDAX
description: Cargar valor de memoria en el acumulador.
layout: ../../../layouts/MainLayout.astro
---

import Solution from '../../../components/Solution'

Cargar valor de memoria en el acumulador.

A diferencia de la operación [`LDA`](LDA), ésta no espera un valor inmediato, ya que coge la dirección de memoria de los registros RD-RE.

**El resultado se ve reflejado el acumulador**

```
AC = READMEM(RD-RE)
```


### Comando

```
LDAX
```

## Comprueba tus conocimientos

Crea un programa que guarde en RB el contenido de la dirección de memoria 0x1324.

**Nota**: debes conocer y utilizar la operación: [`MOV`](MOV)

<Solution title="Solución" sol="MOV 13, RD\nMOV 24, RE\nLDAX\nMOV AC, RB" testCodeText="Probar código" client:load />